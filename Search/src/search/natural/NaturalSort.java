package search.natural;

//자연 정렬을 하기 위해서는 다음과 같은 패턴으로 클래스를 정의
/*
public class NaturalSort implements Comparable<T> {

    public int compareTo(T c) {
        //this가 c보다 크면 양의 값 반환
        //this가 c보다 작으면 음의 값 반환
        //this가 c와 같으면 0 반환
    }

    public boolean equals(Object c) {
        //this가 c와 같으면 true반환
        //this가 c와 같지 않으면 false 반환
    }
}
*/
